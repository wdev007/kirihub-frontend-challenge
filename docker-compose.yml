version: "3.8"

services:
  frontend:
    restart: always
    build: .
    volumes:
      - ".:/usr/src/app"
    env_file:
      - .env
    ports:
      - ${REACT_APP_PORT}:${REACT_APP_PORT}
    environment:
      - NODE_ENV=development
